<ul>
<% @events.each do |event| %>
  <li>
 	  <%= event.name %>
  <%= link_to '详情', :controller => 'events', :action => 'show', :id => event %>
  <%= link_to '编辑', :controller => 'events', :action => 'edit', :id => event %>
	<%= link_to '删除', :controller => 'events', :action => 'destroy', :id => event%>
  </li>
<% end %>
<br />
<br />
<%= paginate @events %>
</ul>
<%= link_to '新建 Event', :controller => 'events', :action => 'new' %>
